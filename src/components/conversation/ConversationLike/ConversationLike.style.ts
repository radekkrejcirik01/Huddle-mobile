import { StyleSheet } from 'react-native';

export const ConversationLikeStyle = StyleSheet.create({
    heartView: {
        marginRight: 30
    },
    heartText: {
        fontSize: 20
    },
    heartIcon: {
        marginRight: 31,
        opacity: 0.8
    }
});

import { StyleSheet } from 'react-native';

export const ConversationLikeStyle = StyleSheet.create({
    heartView: {
        marginRight: 20
    },
    heartText: {
        fontSize: 20
    },
    heartIcon: {
        marginRight: 21,
        opacity: 0.8
    }
});

import { StyleSheet } from 'react-native';

export const FriendsScreenStyle = StyleSheet.create({
    container: {
        flex: 1,
        paddingTop: 15
    },
    input: {
        paddingLeft: 10,
        fontSize: 14,
        fontWeight: '500'
    }
});

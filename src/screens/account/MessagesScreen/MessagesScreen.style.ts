import { StyleSheet } from 'react-native';

export const MessagesScreenStyle = StyleSheet.create({
    container: {
        flex: 1,
        marginTop: 10
    },
    contentContainer: {
        paddingHorizontal: 5
    }
});

import { StyleSheet } from 'react-native';

export const ProfilePhotoScreenStyle = StyleSheet.create({
    scrollView: {
        marginTop: 75,
        flexGrow: 0,
        height: '70%',
        borderRadius: 25
    }
});

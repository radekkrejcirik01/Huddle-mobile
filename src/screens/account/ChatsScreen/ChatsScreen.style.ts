import { StyleSheet } from 'react-native';

export const ChatsScreenStyle = StyleSheet.create({
    container: {
        flex: 1
    },
    contentContainer: {
        paddingTop: 15,
        paddingHorizontal: 5
    }
});

import { StyleSheet } from 'react-native';

export const NotificationsScreenStyle = StyleSheet.create({
    container: {
        paddingHorizontal: 5
    },
    flashListView: {
        width: '100%',
        height: '100%',
        marginTop: 5
    }
});

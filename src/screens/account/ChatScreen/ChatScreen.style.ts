import { StyleSheet } from 'react-native';

export const ChatScreenStyle = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%'
    },
    image: {
        width: 35,
        height: 35,
        marginRight: 15,
        borderRadius: 20
    }
});
